apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: session-redis-data
  name: session-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
